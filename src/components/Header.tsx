import React from "react";
import { Headers } from "./styles/RegistrationStyles";

const Header = () => {
  return (
    <Headers>
      <div>Registration Form</div>
    </Headers>
  );
};

export default Header;
